# Secrets — never commit these
.env
*.env
.env.*

# Python
__pycache__/
*.pyc
.venv/
venv/

# Node / TypeScript
node_modules/
dist/
.next/
*.tsbuildinfo
*.js.map

# Logs + data
logs/
*.log
seen_ids.json
cache_articles.json
cache_signals.json

# Runtime signal bus — live JSON state, not source. Changes constantly; never commit.
signals/*.json
signals/*.bak

# Backups / archives
.backups/
*.bak

# Lock file
.pipeline.lock

# OS
.DS_Store

# Backtest data cache (regenerable from Yahoo)
quant/data_cache/

# live runner local env (paper keys)
quant/.env
quant/live_logs/

# equity protector runtime state (never commit)
quant/KILL_SWITCH.json
quant/protector_state.json

# research platform caches
quant/research/**/__pycache__/
quant/research/experiments/*/results.json
